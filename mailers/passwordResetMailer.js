const nodemailer = require('nodemailer');

const sendPasswordResetEmail = (email, resetLink) => {
  // Логика для отправки электронного письма с ссылкой на сброс пароля
  // Используйте nodemailer или другую библиотеку для отправки электронных писем
};

module.exports = { sendPasswordResetEmail };
